package hotciv.standard;

import hotciv.framework.*;

// introduce PlayerSetup interface
public class GenericPlayerSetup implements PlayerSetup {
    @Override
    public void setupPlayer(GameImpl game) {
        // do nothing
    }
}
